# Multithreaded-WebServer
A simple multithreaded web server implemented in Java using core multithreading concepts. The server uses ServerSocket to listen for incoming client connections on a specified port. When a client connects, the server accepts the connection via a Socket and creates a new thread to handle that client independently.
